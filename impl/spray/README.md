[Spray](http://spray.io/) implementation
========================================

[SBT](http://www.scala-sbt.org/) is used to build this project

First build the project with:

`sbt assembly`

Then to run the application for testing:

`java -jar target/spray-impl.jar`